\usepackage[
    a4paper,
    vmargin=25mm,
    hmargin=30mm,
    headheight=13pt,
    nomarginpar
]{geometry}

\ifbool{TEMPLATE@padding}{
    \geometry{
        hmargin=10mm,
        bindingoffset=20mm,
    }

    \ifbool{TEMPLATE@showpad}{
        \AddToHook{shipout/background}{%
            \begin{tikzpicture}[remember picture,overlay,very thin,gray!20]
                \coordinate (TL) at (current page.north west);
                \coordinate (BL) at (current page.south west);

                \filldraw (TL) -- ([xshift=+20mm] TL) -- ([xshift=+20mm] BL) -- (BL);
            \end{tikzpicture}
        }
    }{}
}{}

\usepackage{lastpage}
\usepackage{fancyhdr}

\fancypagestyle{blank}{
    \fancyhf{}
    \renewcommand{\headrulewidth}{0pt}
    \renewcommand{\footrulewidth}{0pt}
}

\fancypagestyle{plain}{
    \fancyhf{}

    \fancyfoot[C]{\thepage}

    \renewcommand{\headrulewidth}{0.4pt}
    \renewcommand{\footrulewidth}{0.4pt}
}

\fancypagestyle{content}[plain]{
    \fancyhead[R]{\leftmark}
}

\usepackage{pdflscape}
% \usepackage{lscapeenhanced}
